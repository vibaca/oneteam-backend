version: '3.8'
services:
  postgres:
    image: postgres:15-alpine
    container_name: oneteam_postgres
    environment:
      POSTGRES_DB: oneteam_db
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: password
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata: